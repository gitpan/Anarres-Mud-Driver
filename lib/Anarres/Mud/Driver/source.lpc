// #include <lib.h>
#define SYSLOG_D "foo"

#if 0
inherit "/foo/bar";
#endif

/* post this out to a logging daemon in time */
void
syslog(int priority, string type, string message)
{
#if 0
	object   ob;
	string   str;

	ob = find_object(SYSLOG_D);
	str = "[" + file_name(previous_object(0)) + "] " + message;
	if (ob)
		ob->eventLog(priority, type, str);
	else
		debug_message("[syslog] (!SYSLOG_D) " + str);
#endif

	int i;

	i = 0;
	for (; i < 10; i++) {
	}
}
